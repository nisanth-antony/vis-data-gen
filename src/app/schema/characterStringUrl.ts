/**
 * A URL in conformance with IETF RFC 1738. A VIS shall support HTTP redirects when GETting a URL. A VIS shall provide security tokens when GETting a URL.
 */
export type CharacterStringUrl = string;
